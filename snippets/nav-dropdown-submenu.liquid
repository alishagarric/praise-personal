{% comment %}
    Renders a submenu list, originally made to be used in a navigation dropdown

    Expects:
    - menu: {object} required. An array of links
    - label: {string} optional
    - label_link: {url} optional
    - className: {string} optional

    Usage:
    {% render 'nav-dropdown-submenu' menu: menu %}
{% endcomment %}

{% if menu.size > 0 %}
  <div class="{{ className }} __submenu">
    {% if label %}
      {% if label_link and label_link != "#" %}
        <a href="{{ label_link }}" class="{{ className }}__label p-lg">{{ label | escape }}</a>
      {% else %}
        <span class="{{ className }}__label p-lg">{{ label | escape }}</span>
      {% endif %}
    {% endif %}
    <ul class="{{ className }}__submenu-list">
      {% for link in menu %}
        <li>
          <a href="{{ link.url }}" class="p-sm">
            {{ link.title | escape}}
          </a>
        </li>
      {% endfor %}
    </ul>
    </span>
  </div>
{% endif %}