{% style %}
  #Custom-liquid-{{ section.id }} {
    --topSpacingRatio: {{ section.settings.top_spacing }} / 100;
    --bottomSpacingRatio: {{ section.settings.bottom_spacing }} / 100;
  }
{% endstyle %}

{% if section.settings.output_section %}
  <div
    id="Custom-liquid-{{ section.id }}"
    class="custom-liquid  __theme-{{ section.settings.theme }}"
  >
    {{ section.settings.custom_liquid }}
  </div>
{% else %}
  {{ section.settings.custom_liquid }}
{% endif %}

{% schema %}
{
  "name": "t:sections.custom-liquid.name",
  "tag": "section",
  "settings": [
    {
      "type": "liquid",
      "id": "custom_liquid",
      "label": "t:sections.custom-liquid.settings.custom_liquid.label",
      "info": "t:sections.custom-liquid.settings.custom_liquid.info"
    },
    {
      "type": "checkbox",
      "id": "output_section",
      "label": "t:sections.custom-liquid.settings.output_section.label",
      "default": true,
      "info": "t:sections.custom-liquid.settings.output_section.info"
    },
    {
      "type": "range",
      "id": "top_spacing",
      "min": 0,
      "max": 100,
      "step": 10,
      "unit": "%",
      "label": "t:sections.custom-liquid.settings.top_spacing.label",
      "default": 50
    },
    {
      "type": "range",
      "id": "bottom_spacing",
      "min": 0,
      "max": 100,
      "step": 10,
      "unit": "%",
      "label": "t:sections.custom-liquid.settings.bottom_spacing.label",
      "default": 50
    },
    {
      "type": "select",
      "id": "theme",
      "options": [
        {
          "value": "primary",
          "label": "t:sections.custom-liquid.settings.theme.options.label_1"
        },
        {
          "value": "2",
          "label": "t:sections.custom-liquid.settings.theme.options.label_2"
        },
        {
          "value": "3",
          "label": "t:sections.custom-liquid.settings.theme.options.label_3"
        },
        {
          "value": "4",
          "label": "t:sections.custom-liquid.settings.theme.options.label_4"
        },
        {
          "value": "5",
          "label": "t:sections.custom-liquid.settings.theme.options.label_5"
        }
      ],
      "default": "primary",
      "label": "t:sections.custom-liquid.settings.theme.label"
    }
  ],
  "presets": [
    {
      "name": "t:sections.custom-liquid.presets.name"
    }
  ]
}
{% endschema %}
