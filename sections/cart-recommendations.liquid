{{ 'cart-recommendations.css' | asset_url | stylesheet_tag }}

<script src="{{ 'cart-recommendations.js' | asset_url }}" defer="defer"></script>

{% style %}
  #Cart-recommendations-{{ section.id }} {
    --topSpacingRatio: {{ section.settings.top_spacing }} / 100;
    --bottomSpacingRatio: {{ section.settings.bottom_spacing }} / 100;
  }
{% endstyle %}

<div id="Cart-recommendations-{{ section.id }}" class="cart-recommendations __theme-{{ section.settings.theme }}">
  <cart-recommendations data-url="{{ routes.product_recommendations_url }}?section_id={{ section.id }}&product_id={{ cart.items.first.product.id }}&limit=3">
    {% if recommendations.performed and recommendations.products_count > 0 %}
      {% comment %} Text Content {% endcomment %}
      {% render 'heading',
        class: 'cart-recommendations',
        heading: section.settings.heading,
        alt_heading: true,
        heading_size: 'p-sm'
      %}

      <ul class="cart-recommendations__container list-unstyled" role="list">
        {% for recommendation in recommendations.products %}
          <li>
            {% render 'quick-product',
              quick_product: recommendation,
              img_width: 260,
              img_widths: '260, 150',
              img_sizes: '(max-width: 1024px) 150px, 10vw'
            %}
          </li>
        {% endfor %}
      </ul>
    {% endif %}
  </cart-recommendations>
</div>

{% schema %}
{
  "name": "t:sections.cart-recommendations.name",
  "tag": "section",
  "settings": [
    {
      "type": "header",
      "content": "t:sections.cart-recommendations.settings.header.content"
    },
    {
      "type": "text",
      "id": "heading",
      "default": "You may also like",
      "label": "t:sections.product-recommendations.settings.heading.label"
    },
    {
      "type": "header",
      "content": "t:sections.cart-recommendations.settings.header_2.content"
    },
    {
      "type": "select",
      "id": "theme",
      "options": [
        {
          "value": "primary",
          "label": "t:sections.cart-recommendations.settings.theme.options.label_1"
        },
        {
          "value": "2",
          "label": "t:sections.cart-recommendations.settings.theme.options.label_2"
        },
        {
          "value": "3",
          "label": "t:sections.cart-recommendations.settings.theme.options.label_3"
        },
        {
          "value": "4",
          "label": "t:sections.cart-recommendations.settings.theme.options.label_4"
        },
        {
          "value": "5",
          "label": "t:sections.cart-recommendations.settings.theme.options.label_5"
        }
      ],
      "default": "primary",
      "label": "t:sections.cart-recommendations.settings.theme.label"
    },
    {
      "type": "range",
      "id": "top_internal_spacing",
      "min": 0,
      "max": 300,
      "step": 10,
      "unit": "%",
      "label": "t:sections.cart-recommendations.settings.top_internal_spacing.label",
      "default": 100
    },
    {
      "type": "range",
      "id": "bottom_internal_spacing",
      "min": 0,
      "max": 300,
      "step": 10,
      "unit": "%",
      "label": "t:sections.cart-recommendations.settings.bottom_internal_spacing.label",
      "default": 100
    },
    {
      "type": "range",
      "id": "top_spacing",
      "min": 0,
      "max": 100,
      "step": 10,
      "unit": "%",
      "label": "t:sections.cart-recommendations.settings.top_spacing.label",
      "default": 50
    },
    {
      "type": "range",
      "id": "bottom_spacing",
      "min": 0,
      "max": 100,
      "step": 10,
      "unit": "%",
      "label": "t:sections.cart-recommendations.settings.bottom_spacing.label",
      "default": 50
    }
  ],
  "presets": [
    {
      "name": "t:sections.cart-recommendations.presets.name"
    }
  ]
}
{% endschema %}
